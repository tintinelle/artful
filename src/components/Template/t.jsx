import style from "./styles.module.scss";

const T = ({ displayModalForm }) => {
  // onClick={() => displayModalForm()}
  return (
    <section className={style[""]}>
      <div className={style["__"]}></div>
    </section>
  );
};

export default T;
